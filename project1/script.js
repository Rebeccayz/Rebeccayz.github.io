var elements = document.getElementsByClassName('disappear')



/*
function drawRectangles(n){
  for (i = 0; i < elements.length; i++){
    var child = document.createElement("div"); //creating a new div
    child.className = "element"; //assigning a class name to the div
    parent.append(child); // appending the div to the element 'parent'
  }
}

function Get(url){
    var Httpreq = new XMLHttpRequest(); // a new request
    Httpreq.open("GET",url,false);
    Httpreq.send(null);
    return Httpreq.responseText;
}
*/
